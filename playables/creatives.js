/**
 * Every creative in this repo. The build, the harness and the publish step all
 * read this list — there is no directory scan.
 *
 * Each entry is a folder under `creatives/` with its own `index.html` and `src/`.
 * The shared engine and the ad-network layer live one level up in `shared/`.
 */
export const CREATIVES = [
  { id: 'marble-run', label: 'Marble Rush' },
  { id: 'sort-3d', label: 'Shelf Sort' },
]
